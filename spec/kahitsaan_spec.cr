require "./spec_helper"

describe Kahitsaan do
  # TODO: Write tests
  # Maybe later, procrastinate... wait, is it procrastinating if you're only writing a tool because you're bored?

  it "works" do
    false.should eq(true)
  end
end
